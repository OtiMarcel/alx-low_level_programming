#include "main.h"
/**
* _isalpha - prints if its a letter
* @c: is a single input
*
* Return: 1 if its a letter, 0 if not
*/
int _isalpha(int c)
{
	if (c >= 'a' || c >= 'A')
	return (1);

	else
	return (0);
}
