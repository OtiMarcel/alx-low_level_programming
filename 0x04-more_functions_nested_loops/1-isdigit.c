#include "main.h"
/**
*_isdigit - checks if input is a digit
*@c: single input
*
* Return: 1 for digit and 0 if otherwise
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
{
	return (1);
}
	else
{
	return (0);
}
}
