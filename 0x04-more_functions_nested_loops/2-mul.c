#include "main.h"
/**
*  mul - computes the multiplication
* of two integers
* @a: first number
* @b: second number
*
* Return: product of two numbers (a and b)
*/
int mul(int a, int b)
{
	return (a * b);
}
