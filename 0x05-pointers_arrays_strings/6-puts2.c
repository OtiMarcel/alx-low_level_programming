#include "main.h"
#include <string.h>
/**
* puts2 - prints out every other character 
* of a string
*@str: input string
*
* Return: void
*/
void puts2(char *str)
{
	int i, len;

	len = strlen(str);

	for (i = 0; i < len; i++)
{
	_puts(str[i];
}
}
