#include <stdio.h>
#include <stdlib.h>

/**
* main - Entry
*
* Return: 0 (success)
*/
int main(void)
{
	int NUM;

	for (NUM = 0; NUM < 10; NUM++)
	putchar('0' + NUM);

	putchar('\n');

	return (0);
}
